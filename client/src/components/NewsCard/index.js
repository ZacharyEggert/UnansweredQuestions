import React from 'react';
import { Link } from 'react-router-dom';

const NewsCard = () => {
    return (
        <div className="flex flex-row items-center flex-initial w-full"></div>
    );
};

export default NewsCard;
